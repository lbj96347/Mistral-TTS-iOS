name: VoxtralTTS
options:
  bundleIdPrefix: com.voxtral
  deploymentTarget:
    iOS: "17.0"
    macOS: "14.0"
  xcodeVersion: "15.0"
  generateEmptyDirectories: true

packages:
  mlx-swift:
    url: https://github.com/ml-explore/mlx-swift
    from: "0.21.0"
  swift-transformers:
    url: https://github.com/huggingface/swift-transformers
    from: "0.1.12"

targets:
  VoxtralTTS:
    type: application
    platform: [iOS, macOS]
    sources:
      - path: VoxtralTTS/Sources
    resources:
      - path: VoxtralTTS/Resources
    info:
      path: VoxtralTTS/Info.plist
      properties:
        NSMicrophoneUsageDescription: "Record your voice to create a custom voice clone for text-to-speech."
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.voxtral.tts
        MARKETING_VERSION: "1.0"
        CURRENT_PROJECT_VERSION: "1"
        SWIFT_VERSION: "5.9"
        GENERATE_INFOPLIST_FILE: false
        CODE_SIGN_STYLE: Automatic
        SUPPORTS_MACCATALYST: false
      configs:
        Debug:
          SWIFT_OPTIMIZATION_LEVEL: -Onone
          CODE_SIGN_ENTITLEMENTS: VoxtralTTS/VoxtralTTS.debug.entitlements
        Release:
          SWIFT_OPTIMIZATION_LEVEL: -O
          CODE_SIGN_ENTITLEMENTS: VoxtralTTS/VoxtralTTS.entitlements
    dependencies:
      - package: mlx-swift
        product: MLX
      - package: mlx-swift
        product: MLXFast
      - package: mlx-swift
        product: MLXNN
      - package: mlx-swift
        product: MLXRandom
      - package: swift-transformers
        product: Transformers

  VoxtralTTSTests:
    type: bundle.unit-test
    platform: [macOS]
    sources:
      - path: VoxtralTTSTests
      - path: VoxtralTTS/Sources
        excludes:
          - "App/VoxtralTTSApp.swift"
    settings:
      base:
        SWIFT_VERSION: "5.9"
        GENERATE_INFOPLIST_FILE: true
    dependencies:
      - package: mlx-swift
        product: MLX
      - package: mlx-swift
        product: MLXFast
      - package: mlx-swift
        product: MLXNN
      - package: mlx-swift
        product: MLXRandom
      - package: swift-transformers
        product: Transformers
